# 应用服务器项目 SMTP 配置示例
# 注意：这里填写的是 Relay 账号密码，不是上游 SMTP 密码/API Key。

SMTP_HOST=中继服务器IP或灰云域名
SMTP_PORT=2525
SMTP_USER=relay
SMTP_PASS=replace-with-relay-password
SMTP_SECURE=false
SMTP_STARTTLS=true

# 发件地址必须是上游 SMTP 服务商允许的发件地址
MAIL_FROM_ADDRESS=verified-sender@example.com
MAIL_FROM_NAME=App
